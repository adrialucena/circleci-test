version: 2
jobs:
  build:
    docker:
      - image: debian:stretch

    steps:
      - checkout

      - run:
          name: tests
          command: npm run test

      - run:
          name: Print the Current Time
          command: date
